// Package zen is a tiny micro-framework built on top of the standard
// library&#39;s `net/http`. It offers a small set of helpers (context type,
// graceful shutdown, JSON/form helpers) but leaves routing and middleware to
// whatever you prefer.

package zen
